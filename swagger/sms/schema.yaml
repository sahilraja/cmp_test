responses:
  templateME:
    description: template schema
    content:
      application/json:
        schema:
          $ref: "#/template/templateSchema"
  schemas:
    $ref: ../error.yaml
      # user schema
    smsTemplateSchema:
      type: object
      properties:
        content: 
          type: string
          example: subject
        templateName:
          type: string
          example: information
        displayName:
          type: string
          example: Display Name
        